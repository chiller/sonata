import {list, cons, head, tail, isEmpty, show} from "./framework.js";
/* Exercise 6
 * Find out whether a list is a palindrome
 * For example: palindrome(list(1,2,3,2,1)) -> true
 * Hint: you might want to create a helper function equals
 * to compare two lists since a palindrome is literally a list
 * that equals itself reversed (double hint)
 */



